type Query {
  me: User!
  user(id: ID!): User
  users: [User]!
  wallpapers: [Wallpaper]!
}

type Mutation {
    addWallpaper(image: String!): Wallpaper
}

type Wallpaper {
    id: ID!
    image: String!
}

type User {
  id: ID!
  name: String!
}
