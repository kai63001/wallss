type Query {
  me: User!
  user(_id: ID!): User
  users: [User]!
  wallpapers: [Wallpaper]
}

type Mutation {
    addWallpaper(image: String!): Wallpaper
}


type User {
  _id: ID!
  name: String!
}

type Wallpaper {
    _id: ID!
    image: String!
    users: [User] 
}